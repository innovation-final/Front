import { createSlice } from '@reduxjs/toolkit';

/** InitialState */
const initialState = {};

/** THUNK */

/** USERSLICE */
const userSlice = createSlice({
    name: 'user',
    initialState,
    reducers: {},
    extraReducers: {},
});

export default userSlice.reducer;
